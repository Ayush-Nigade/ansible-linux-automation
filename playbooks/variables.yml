---
- name: Implementing Variables
  hosts: webservers
  become: yes

  vars:
    package_name: vim

  tasks:
    - name: Install package using variable
      dnf:
        name: "{{ package_name }}"
        state: present
