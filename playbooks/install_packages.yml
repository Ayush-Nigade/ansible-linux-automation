---
- name: Install Packages
  hosts: webservers
  become: yes

  tasks:
    - name: Install Apache
      yum:
        name: httpd
        state: present

    - name: Install Git
      yum:
        name: git
        state: present
