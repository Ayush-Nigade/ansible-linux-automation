---
- name: Basic Facts
  hosts: webservers

  tasks:
    - name: Show hostname
      debug:
        var: ansible_hostname
    
    - name: Show IP address
      debug:
        var: ansible_default_ipv4.address
